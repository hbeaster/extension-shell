apiVersion: v1
kind: Service
metadata:
  name: {{ include "extensions.fullname" . }}
  labels:
    {{- include "extensions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "extensions.selectorLabels" . | nindent 4 }}

{
  "name": "shell-extensions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "smiley-face",
    "buzzer"
  ],
  "scripts": {
    "build": "npm run build --workspaces && node scripts/assemble.mjs"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  }
}
